Logic Engine

1. to build message anaylzer c++ library:

  $ cd cxx
  $ mkdir build
  $ cd build
  $ cmake ..
  $ make

2. run the LogicEngine.py

  $ ln -s cxx/build/ErrorHandler/RE.so RE.so
  $ python LogicEngine.py
